import { ChannelName, JSONValue } from "../../src/commonInterface/kacheryTypes"

const logMessageToChannel = async (channelName: ChannelName, message: JSONValue) => {
    // not yet implemented
    // const channelConfig = await loadChannelConfig({channelName})
    // if ((channelConfig.logBucketUri) && (channelConfig.googleServiceAccountCredentials)) {
        
    //     channelConfig.googleServiceAccountCredentials
    // }
}

export default logMessageToChannel